<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xsi:schemaLocation="
			http://www.springframework.org/schema/data/jpa
            http://www.springframework.org/schema/data/jpa/spring-jpa.xsd
			http://www.springframework.org/schema/beans
			http://www.springframework.org/schema/beans/spring-beans.xsd
			http://www.springframework.org/schema/tx
			http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- transactionManager -->
    <bean id="transactionManagerMySql" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactoryMySql" />
    </bean>
    <!-- Entity Manager Factory  -->
    <bean id="entityManagerFactoryMySql" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="persistenceUnitManager" ref="persistenceUnitManagerMySql" />
        <property name="persistenceUnitName" value="persistenceUnitMySql" />
    </bean>
    <!-- Persistence Unit Manager -->
    <bean id="persistenceUnitManagerMySql" class="org.springframework.orm.jpa.persistenceunit.DefaultPersistenceUnitManager">
        <property name="persistenceXmlLocations" value="classpath*:META-INF/persistence-mysql.xml" />
        <property name="defaultDataSource" ref="dataSourceMySql" />
    </bean>
    <!-- JNDI -->
    <bean id="dataSourceMySql" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${demo.pruebatecnicamc.jdbc.driverclass}" />
        <property name="url" value="${demo.pruebatecnicamc.jdbc.url}" />
        <property name="username" value="${demo.pruebatecnicamc.jdbc.username}" />
        <property name="password" value="${demo.pruebatecnicamc.jdbc.password}" />
    </bean>

    <!-- fin Source por JNDI MYSQL -->
    <tx:annotation-driven transaction-manager="transactionManagerMySql" />

    <jpa:repositories base-package="com.demo.pruebatecnicamc.be" entity-manager-factory-ref="entityManagerFactory"
                      transaction-manager-ref="transactionManager" />
    <jpa:repositories base-package="com.demo.pruebatecnicamc.be.repositories.mysql" entity-manager-factory-ref="entityManagerFactoryMySql"
                      transaction-manager-ref="transactionManagerMySql" />

</beans>
